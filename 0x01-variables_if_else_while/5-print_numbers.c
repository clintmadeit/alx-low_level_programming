#include <stdio.h>
/**
 * main - entry point
 * program description
 * function that "prints all digits of base 10 from 0"
 *
 * Return: Always 0
 */
int main(void)
{
int n;

while (n >= 0 && n < 10)
	n++;

{
	printf("%d\n", n);
}

return (0);
}
