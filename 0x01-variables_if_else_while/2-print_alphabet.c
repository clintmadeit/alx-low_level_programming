#include <stdio.h>
/**
 * main - Entry point
 *
 * This program executes the putchar function and returns a true output
 * Return:0 (success)
 */
int main(void)
{
char ch;
while (ch = 'a' && ch = < 'z')
ch++;
{
	putchar(ch);
}
putchar("\n")
return (0);
}

