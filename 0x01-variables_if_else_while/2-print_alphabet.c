#include <stdio.h>
/**
 * main - Entry point
 *
 * This program executes the putchar function and returns a true output
 * Return:0 (success)
 */
char main(void)
{
	char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
	putchar(ch);
}
{
putchar("\n");
}
return (0);
}

